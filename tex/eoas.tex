\documentclass[class=llncs, crop=false]{standalone}

\input{preamble.tex}
\input{macros.tex}

\begin{document}
\section{An Abstract Syntax for Eunoia}\label{sec:eunoia}
% fig:eo-syntax is in intro.tex so it floats to the top of page 2
% ---------------------------------------------------------
\Autoref{fig:eo-syntax} defines syntax for the
fragment of Eunoia considered in this document, where the
metavariable $s$ ranges over a set of \emph{symbols}.
%
First, we define grammars for the core term syntax.
%
Each term is either a symbol $s$, an (integer/rational/string)
\emph{literal} $ℓ$, an \emph{application} $\eapp{s}{\vec t}$,
or a \emph{binder application} $\eapp{s}{\epar{\vec ρ}\ t}$.
%
Each \emph{parameter} $ρ$ consists of a symbol $s$, a term
% forward ref to where we discuss typing ??
$t$ (the type of the parameter), and possibly a
\emph{parameter attribute} $ν$.


A distinctive feature of Eunoia is its support for
$n$-ary applications of binary symbols.
%
A \emph{constant attribute} $α$ may be assigned to a
declared symbol to specify how $n$-ary applications
are \emph{elaborated} to binary form
(see \autoref{sec:eo-elab}).
%
After elaboration, every application of a declared
constant $s$ uses the built-in higher-order application
operator $\mtt{\_}$, so that $\eapp{s}{t_1\ t_2}$ becomes
$\eho{\eho{s}{t_1}}{t_2}$;
only built-in and program symbols are applied directly.
%
For example, given $f$ with attribute
$\rcn{t_\nil}$, the \emph{$f$-list} with elements
$\plur t n$ is the nested application
$\eho
	{\eho{f}{t_1}}
	{\eho
		{\eho{f}{\ldots}}
		{\eho{\eho{f}{t_n}}{t_\nil}}}
$.
%
Similarly, $\bd{t_\cons}$ specifies that
a binder application $\eapp{s}{\epar{\vec ρ}\ t}$
is elaborated so that each parameter
$\epar{x_i\ t_i}$ in $\vec ρ$ becomes a term
$\epar{\eo{var}\ \estr{x_i}\ t_i}$
within a $t_\cons$-list.

Eunoia provides a fixed set of
\emph{built-in symbols} (\autoref{fig:eo-syntax})
for manipulating terms.
%
The list operators
$\eo{nil}$, $\eo{cons}$, and $\eo{list\_concat}$
construct and combine $f$-lists;
$\eoreqs{t_1}{t_2}{t_3}$ acts as a guard,
evaluating to $t_3$ only when $t_1$ and $t_2$
are syntactically identical;
and $\eoquote{t}$ marks $t$ as a
\emph{quoted argument} whose value may appear
in the return type of a rule or program.
%
Ethos natively evaluates arithmetic and string
built-ins using machine operations, whereas our
translation delegates to the integers and rationals
of the LambdaPi standard library for encoding
Eunoia literals and their built-in operations
via rewrite rules.

% \begin{example}\label{ex:eo-decls}
% 	In CPC, $\ev{or}$
% 	has attribute $\rcn{\false}$, so
% 	$\eapp{\ev{or}}{\ev x\ \ev y\ \ev z}$
% 	elaborates to its $\ev{or}$-list form:
% 	$$\eho{\eho{\ev{or}}{\ev x}}{
% 			\eho{\eho{\ev{or}}{\ev y}}{
% 				\eho{\eho{\ev{or}}{\ev z}}{\false}}}$$
% 	Here $\eapp{\eo{nil}}{\ev{or}\ \bool} = \false$
% 	is the nil terminator.
% 	%
% 	Let $t_1 ≔ \eapp{\ev{or}}{\ev x\ \ev y}$ and
% 	$t_2 ≔ \eapp{\ev{or}}{\ev w\ \ev z}$;
% 	then $\eo{list\_concat}$ concatenates
% 	the two $\ev{or}$-lists as follows:
% 	\begin{align*}
% 		 & \eapp{\eo{list\_concat}}{\ev{or}\ t_1\ t_2}
% 		\\={}&
% 		\eapp{\eo{list\_concat}}{\ev{or}}
% 		\\&\qquad
% 		\eho{\eho{\ev{or}}{\ev x}}{\eho{\eho{\ev{or}}{\ev y}}{\false}}
% 		\\&\qquad
% 		{\eho{\eho{\ev{or}}{\ev w}}{\eho{\eho{\ev{or}}{\ev z}}{\false}}}\rlap{)}
% 		\\={}&
% 		\eho{\eho{\ev{or}}{\ev x}}{
% 			\eho{\eho{\ev{or}}{\ev y}}{
% 				\eho{\eho{\ev{or}}{\ev w}}{
% 					\eho{\eho{\ev{or}}{\ev z}}{\false}}}}
% 		\\={}&
% 		\eapp{\ev{or}}{\ev x\ \ev y\ \ev w\ \ev z}
% 	\end{align*}
% \end{example}

\subsection{Signatures and Proof Scripts.}\label{sec:eo-sig}
% ---------------------------------------------------------
\begin{boxfigure}[t!]{fig:eo-commands}
	{Eunoia commands and proof commands.}
	\vspace{4pt}\centerline{$\eoCommandSyntax$}
	\boxfigurehline
	\centerline{$\eoPrfCommandSyntax$}\vspace{4pt}
\end{boxfigure}
% ---------------------------------------------------------

The commands of Eunoia are divided into
\emph{standard commands} and \emph{proof commands}.
%
Hereinafter, a Eunoia \emph{signature} is a list $Δ$
of standard commands, and a \emph{proof script} is a
list $Υ = \prn{\vec δ ⨾ \vec π}$
where $\vec δ$ is a list of standard commands called
the \emph{preamble} and $\vec π$ is a list of
proof commands called the \emph{body} of the script.
%
In practice, a Eunoia-friendly SMT solver provides a
signature $Δ$ encoding the inference rules that may
be used by its proof scripts.
%
Then, the correctness of a proof script $Υ$ may then be
checked wrt. $Δ$ via Ethos.

In the following discussion, we define some terminology
and notation for commands, signatures, and proof scripts.
The resulting interface is useful for
characterizing the elaboration and translation operators
in \autoref{sec:eo-elab} and \autoref{sec:results}.
%
We omit datatype declarations, which are not used
by CPC-MINI.

\subsubsection{Constant Declaration.}
%
Let $δ$ be a \emph{(parameterized) constant declaration}
with symbol $s$, parameters $\vec ρ$, and term $t$.
%
We may write $\prn{δ ⊢ s(\vec ρ) : t}$ to express
that the command $δ$ declares $t$ as the \emph{type} of $s$
with respect to parameters $\vec ρ$.
%
Furthermore, if a constant attribute $α$ is given by $δ$,
we may write $\prn{δ ⊢ s(\vec ρ) ∷ α}$.
%
\subsubsection{Macro Definition.}
%
Let $δ$ be a \emph{macro definition} with
symbol $s$, parameters $\vec ρ$, and term $t$.
%
Then $δ$ declares $t$ as the \emph{definiens} of $s$
wrt. $\vec ρ$; written $\prn{δ ⊢ s(\vec ρ) ≔ t}$.
%
If the attribute $\attr{type}\ t'$ is given by $δ$,
then $\prn{δ ⊢ s(\vec ρ) : t'}$ also holds.
%
\subsubsection{Program Declaration.}
%
Let $δ$ be a \emph{program declaration} with
symbol $s$, parameters $\vec ρ$,
$\attr{signature}\,\prn{\plur t n}\,t'$,
and cases $\plur c m$.
%
Then, the type and definiens of $s$ is given thus:
% Let $\vec t = \plur t n$ and $\vec r = \plur r m$,
% and $δ ∈ Δ$.
% $(\plurcomma t n)$, \emph{range} $t'$ and
% \emph{cases} $\vec r$ with respect to $\vec ρ$.
%
% More formally, we may write:
$$
	δ ⊢ s(\vec ρ) : \earr{\plur t n\, t'}
	\quad\text{and}\quad
	δ ⊢ s(\vec ρ) ≔ \mbf{cases}[c_1, \ldots, c_m]
$$
%
\subsubsection{Proof Commands.}
Each $π$ is either an \emph{assumption}
(i.e., $\epar{\kw{assume}\ s\ φ}$ or
$\epar{\kw{assume-push}\ s\ φ}$)
or a \emph{step}
(i.e.,
$\epar{\kw{step}\ s\ φ\ \ldots}$ or
$\epar{\kw{step-pop}\ s\ φ\ \ldots}$).
%
In any case, we call
$s$ the \emph{name} of $π$ and $φ$ the \emph{conclusion},
written:
$$π ⊢ s : \eapp{\mtt{Proof}}{φ}$$
%
When $π$ is a step with
$\attr{rule}\ r$,
the definiens of $s$ is the result of
applying any terms given by
$\attr{args}\ \epar{\plur t m}$,
and
$\attr{premises}\ \epar{\plur p n}$, e.g.;
%
$$
	π ⊢ s ≔ \eapp{r}{\plur t m\ \plur p n}
$$
%
\subsubsection{Rule Declaration.}
%

% --- Detailed rule typing (saved for sec:results) ---
Let $δ$ be a \emph{rule declaration} with symbol $s$,
parameters $\vec ρ$, and $\conclusion φ$.
%
By considering the attributes that may
be supplied by $δ$, an appropriate type for $s$
can be derived as follows:
\begin{enumerate}
	\item Given
	      $\requires{\ecase{l_1}{r_1}\ldots\ecase{l_k}{r_k}}$,
	      define $φ^★$ below. Otherwise, $φ^★ ≔ φ$.
	      $$φ^★ ≔ \eoreqs{l_1}{r_1}{
			      \epar{
				      \,\ldots\,\eoreqs{l_k}{r_k}{φ}}
		      }
	      $$

	\item If $δ$ provides no premises, let $τ^★ ≔ \eprf{φ^★}$.
	      Otherwise, define $τ^★$ thus:
	      \begin{enumerate}
		      \item Given
		            $\attr{premises}\ \epar{\plur ψ m}$,
		            let:
		            $$τ^★ ≔ \earr{\eprf{ψ_1}\,\ldots\,\eprf{ψ_m}\ \eprf{φ^★}}$$
		            %
		      \item Given $\premiselist ψ f$ for
		            some associative symbol $f$, redefine:
		            $$φ^★ ≔\eoreqs{\eapp{\eo{is\_list}}{f\ ψ}}{\ev{true}}{φ^★}$$
		            and let $τ^★ ≔ \earr{\eprf{ψ}\ \eprf{φ^★}}$.
	      \end{enumerate}

	\item If $δ$ provides $\attr{args}\ \epar{\plur t n}$,
	      then the type of $s$ is given thus:
	      $$δ ⊢
		      s(\vec ρ)
		      : \earr{\eoquote{t_1}\,\ldots\,\eoquote{t_n}\ τ^★}
	      $$
	      Otherwise, $δ ⊢ s(\vec ρ): τ^★$.
\end{enumerate}
% --- end saved rule typing ---

\begin{figure}[t!]
	\begin{eobox}
		\lstinputlisting[style=eo, aboveskip=0pt, belowskip=0pt]{listings/rule-modus-ponens.eo}%
		\listsep
		\lstinputlisting[style=eo, aboveskip=0pt, belowskip=0pt]{listings/rule-reordering.eo}%
		\listsep
		\lstinputlisting[style=eo, aboveskip=0pt, belowskip=0pt]{listings/rule-cnf-implies-pos.eo}%
		\listsep
		\lstinputlisting[style=eo, aboveskip=0pt, belowskip=0pt]{listings/rule-and-intro.eo}%
	\end{eobox}
	\caption{Rule declarations from
		\texttt{cpc/rules/Booleans.eo},
		see \autoref{ex:eo-rules}.}
	\label{fig:cpc-rules}
\end{figure}

\begin{example}\label{ex:eo-rules}
	Consider the inference rule declarations in
	\autoref{fig:cpc-rules}.
	%
	\noindent
	The $\ev{modus\_ponens}$ rule has only
	$\attr{premises}$ and $\attr{conclusion}$.
	By item 2a:
	$$
		\ev{modus\_ponens}(\vec ρ)  :
		{\color{eoPunct}{\texttt{(}}}\ev{->}\
		\eprf{\ev{F1}}\
		\eprf{\eapp{\ev{=>}}{\ev{F1}\ \ev{F2}}}\
		\eprf{\ev{F2}}
		{\color{eoPunct}{\texttt{)}}}
	$$
	%
	\noindent
	The declaration of $\ev{reordering}$ additionally uses
	$\attr{args}$ and $\attr{requires}$.
	%
	By item~3, $\ev{C2}$ becomes an $\lpo{quote}$-type,
	and item~1 guards the conclusion
	thus $\ev{C2}^★ ≔ \eoreqs
		{\eapp{\eo{list\_minclude}}{\ev{or}\ \ev{C1}\ \ev{C2}}}
		{\ev{true}}{\ev{C2}}$.
	Then:
	$$
		\ev{reordering}(\vec ρ)  :
		{\color{eoPunct}{\texttt{(}}}\ev{->}\
		\eoquote{\ev{C2}}\
		\eprf{\ev{C1}}\
		\eprf{\ev{C2}^★}
		{\color{eoPunct}{\texttt{)}}}
	$$
	%
	\noindent
	The declaration of $\ev{cnf\_implies\_pos}$
	has $\attr{args}\ \epar{\eapp{\ev{=>}}{\ev{F1}\ \ev{F2}}}$,
	where the argument is a compound term.
	By item~3, the $\lpo{quote}$-typed parameter
	demands pattern matching on this term
	at the point of use,
	where $φ$ is the $\attr{conclusion}$:
	$$
		\ev{cnf\_implies\_pos}(\vec ρ)  :
		{\color{eoPunct}{\texttt{(}}}\ev{->}\
		\eoquote{\eapp{\ev{=>}}{\ev{F1}\ \ev{F2}}}\
		\eprf{φ}
		{\color{eoPunct}{\texttt{)}}}
	$$
	%
	%
	\noindent
	Finally, $\ev{and\_intro}$ uses
	$\premiselist{\ev F}{\ev{and}}$.
	%
	By item~2b, the conclusion is guarded thus,
	$\ev{F}^★ ≔ \eoreqs
		{\eapp{\eo{is\_list}}{\ev{and}\ \ev{F}}}
		{\ev{true}}{\ev{F}}$. Hence:
	$$
		\ev{and\_intro}(\vec ρ)  :
		{\color{eoPunct}{\texttt{(}}}\ev{->}\
		\eprf{\ev{F}}\
		\eprf{\ev{F}^★}
		{\color{eoPunct}{\texttt{)}}}
	$$
\end{example}

% where each $t_i$ has type $τ_i$ with respect to the
% current context $\prn{Δ, \vec ρ}$.
% %
% We introduce an auxilliary symbol
% $s^★$ with the following properties:
% $$
%  \begin{array}[t]{l@{\ ⊢\ }l}
%   δ & s^★(\vec ρ) : \earr{\plur τ n\ \etype}
%   \\[1mm]
%   δ & s^★(\vec ρ) ≔ \mbf{cases}
%   [
%    \epar{{\eapp{s^★}{\plur t n}}\ τ^★}
%   ]
%  \end{array}
% $$
% where $\vec v ≔
% %  \vec ρ ⨾ \epar{α_1\ τ_1}\,\ldots\,\epar{α_n\ τ_n}$.
% 	      The use of the auxilliary symbol ensures that the
% 	      `logical part' (i.e., the premises and conclusion)
% 	      of the rule may be obtained iff the given
% 	      arguments match the `patterns' specified by each $t_i$.

\subsection{Elaboration of Terms}\label{sec:eo-elab}
%
Terms are \emph{elaborated} to eliminate all $n$-ary
applications and distinguish between constant-level and
program-level function applications.
%
The elaborated form of $\eapp{f}{\vec t}$
is determined by the attribute assigned to $f$ by $Δ$.
%
% %
% For example, consider a constant declaration $δ$ with:
% $$
% 	δ ⊢ \eor : \earr{\bool\ \bool\ \bool}
% 	\quad\text{and}\quad
% 	δ ⊢ \eor ∷ {\rcn \false}
% $$
% % and $δ ⊢ $.
% % $$\dca{\eor}{\earr{\bool\ \bool\ \bool}}$$
% %
% From the type of $\eor$, we may expect its only valid
% uses to be of the form $\eapp{\eor}{t_1\ t_2}$.
% %
% However, the assignment of the constant attribute means
% that the $\mtt{or}$ symbol is treated as
% \emph{right-associative} with \emph{nil-terminator} $\false$.
% %
% Thus, $n$-ary applications of $\eor$ are
% \emph{elaborated} to a normal form, e.g.;
% %
% $$\eapp{\eor}{\ev x\ \ev y\ \ev z}\ ⇢\
% 	\eapp{\eor}{\ev x\ \eapp{\eor}{\ev y\ \eapp{\eor}{\ev z\ \false}}}$$
% %
% Furthermore, the elaboration of such applications may
% also depend on the attributes of `locally bound' symbols.
% For example, consider $\vec ρ$ containing
% parameters $\epar{\ev x\ \bool}$,
% $\epar{\ev y\ \bool\ \mtt{:list}}$,
% and $\epar{\ev z\ \bool}$.
% %
% Then, observe:
% $$
% 	\eapp{\eor}{\ev x\ \ev y\ \ev z}
% 	\ {⇢}
% 	\ \econs\eor{\ev x}{
% 		\econcat{\eor}{\ev y}{\eapp{\eor}{\ev z\ \false}}
% 	}
% $$
% %
% Here, $\eo{list\_concat}$ is a built-in ternary operator:
% $\eapp{\eo{list\_concat}}{f\ \ell\ \ell'}$ concatenates
% two $f$-lists $\ell$ and $\ell'$.
% %
% The $\mtt{:list}$ attribute alters the elaboration strategy
% under the assumption that $\ev y$ will (eventually) be
% substituted for some $\eor$-list.
% %
% It may be helpful for the reader
% to consider the result of substituting
% $\ev y ↦ \econs\eor{\ev w}{\false}$
% thus:
% $$
% 	\begin{array}[t]{c}
% 		\econs\eor{\ev x}{
% 			\econcat{\eor}{\econs\eor{\ev w}{\false}}
% 			{\eapp{\eor}{\ev z\ \false}}}
% 		\\[1mm] {⇣} \\[1mm]
% 		\econs\eor{\ev x}{
% 			\econs\eor{\ev w}{
% 				\econs\eor{\ev z}{\false}}
% 		}
% 	\end{array}
% $$
% %
We define the $\mbf{elab}$ operator below with the aim
of supporting the elaboration strategies
corresponding to the constant attributes of
\autoref{fig:eo-syntax}.
%
\newcommand{\body}{\text{body}}
\newcommand{\vars}{\text{vars}}
\begin{definition}
	For any $Δ$ and $\vec ρ$, let $\elab{Δ,\vec ρ}$
	be the least operator such that:
	\begin{enumerate}
		\item For any symbol $s$, $\elab{Δ,\vec ρ}[s] = s$.
		      %
		\item For any binder application
		      $\eapp{s}{\epar{\vec v}\ t}$
		      where
		      $Δ ⊢ s ∷ \bd t_\cons$,
		      $$ \elab{Δ,\vec ρ}[\eapp{s}{\epar{\vec v}\ t}]
			      =
			      \eapp{s}{t_\vars\ t_\body}
		      $$
		      where $t_\vars$ encodes
		      $\vec v = \epar{x_1\ t_1}\ldots\epar{x_n\ t_n}$
		      as a $t_\cons$-list of $\eo{var}$ terms,
		      and $t_\body$ is the result of substituting each
		      $x_i$ in $t$ for its $\eo{var}$ counterpart:
		      %
		      $$
			      \begin{array}{l@{\ {≔}\ }l}
				      t_\vars &
				      \elab{Δ, \vec ρ}[\eapp{t_\cons}{\plur X n}]
				      \\
				      t_\body &
				      \elab{Δ, \vec ρ}
				      [\,
				      \mbf{subst}_{\prn{\vec x, \vec X}}[t]
				      \,]
			      \end{array}
			      \qquad
			      {\small\text{where}\
				      X_i ≔ \epar{\eo{var}\ \estr{x_i}\ t_i}}
		      $$

		      %
		\item For any application $\eapp s {\vec t}$,
		      let $t_i' ≔ \elab{Δ, \vec ρ}[t_i]$ in:
		      \begin{enumerate}
			      \item If $s$ is a built-in or $s ∈ Δ.\msf{prog}$, then
			            $\elab{Δ, \vec ρ}[\eapp{s}{\vec t}] =
				            \eapp{s}{\vec t'}$.
			      \item If $s ∈ \vec ρ$, then
			            $\elab{Δ, \vec ρ}[\eapp{s}{\vec t}] =
				            \eho{\ldots\eho{\eho{s}{{t_1'}}}{\ldots}}{{t_n'}}
			            $.

			      \item If $Δ ⊢ s ∷ α$ for some associative
			            attribute $α$, then:
			            $$
				            \elab{Δ,\vec ρ}[\eapp{s}{\vec t}]\ {=}\
				            \begin{scases}
					            \foldr(G, t_\nil, \vec t')
					            & \tsm{$\prn{α = \rcn{t_\nil}}$}
					            \\
					            \foldl(G, t_\nil, \vec t')
					            & \tsm{$\prn{α = \lcn{t_\nil}}$}
					            \\
					            \foldr(G, t'_n, t'_1\ldots t'_{n-1})
					            & \tsm{$\prn{α = \rc}$}
					            \\
					            \foldl(G, t'_1, t'_2\ldots t'_{n})
					            & \tsm{$\prn{α = \lc}$}
					            % \\
					            % \elab{Δ,\vec ρ}[\eapp{{t_\cons}}{\mbf{chain}(s, \vec t')}]
					            %  &
					            % \tsm{if $Δ ⊢ s ∷ \ch {t_\cons}$,}
					            % \\
					            % \elab{Δ,\vec ρ}[\eapp{{t_\cons}}{\mbf{pairs}(s, \vec t')}]
					            %  &
					            % \tsm{if $Δ ⊢ s ∷ \pw {t_\cons}$,}
					            % \\
					            % \elab{Δ,\vec ρ}[\eapp{{t_\cons}}{\vec t'}]
					            %  &
					            % \tsm{if $Δ ⊢ s ∷ \al {t_\cons}$,}
					            % \\[1mm]
					            %
					            %  &
					            % \tsm{otherwise.}
				            \end{scases}
			            $$
			            where\footnote{%
				            $\foldl(G, a, l)$ and $\foldr(G, a, l)$ are the
				            standard left and right folds using combining
				            function $G$ and accumulator $a$ on list $l$.
			            }%
			            $G(x,y) ≔ \glue{\vec ρ, s}[x,y]$ is defined as
			            follows:
			            $$
				            \begin{array}[t]{r@{\ ≔\ }l}
					            \glue{\vec ρ, s}[x,y]
					             &
					            \begin{scases}
						            \econcat{s}{x}{y} & \tsm{$\prn{\vec ρ ⊢ x ∷ \attr{list}}$}
						            \\
						            \eho{\eho{s}{x}}{y}
						            & \tsm{otherwise.}
					            \end{scases}
				            \end{array}
			            $$

			      \item If $Δ ⊢ s ∷ α$ for some non-associative
			            attribute $α$, then:
			            $$
				            \elab{Δ,\vec ρ}[\eapp{s}{\vec t}]\ {=}\
				            \elab{Δ,\vec ρ}[T]
			            $$
			            where $T$ is defined using
			            $\mbf{chain}$ and $\mbf{pairs}$
			            thus:
			            $$
				            T ≔
				            \begin{scases}
					            \eapp{t_\cons}{\mbf{chain}(s, \vec t)}
					            &
					            \tsm{$\prn{α = \ch {t_\cons}}$}
					            \\
					            \eapp{t_\cons}{\mbf{pairs}(s, \vec t)}
					            &
					            \tsm{$\prn{α = \pw {t_\cons}}$}
					            \\
					            \eapp{t_\cons}{\vec t}
					            &
					            \tsm{$\prn{α = \al {t_\cons}}$}
				            \end{scases}
			            $$
			            $$
				            \begin{array}[t]{r@{\ {≔}\ }l}
					            \mbf{chain}(s,\, t_1\ldots t_n)
					             & \eapp{s}{t_1\ t_2}\,
					            \eapp{s}{t_2\ t_3}
					            \,\ldots\,
					            \eapp{s}{t_{n-1}\ t_n}
					            \\[1mm]
					            \mbf{pairs}(s,\, t_1\ldots t_n)
					             & \eapp{s}{t_1\ t_2}
					            \,\ldots\,
					            \eapp{s}{t_1\ t_n}\,
					            \eapp{s}{t_2\ t_3}
					            \,\ldots\,
					            \eapp{s}{t_{n-1}\ t_n}
				            \end{array}
			            $$

		      \end{enumerate}

		      %

	\end{enumerate}

	%


\end{definition}

\begin{example}\label{ex:elab}
	Since $Δ ⊢ \ev{or} ∷ \rcn\false$,
	item~3c applies with
	$G(x,y) = \eho{\eho{\ev{or}}{x}}{y}$
	and the right fold gives:
	\begin{align*}
		 & \elab{Δ, \vec ρ}[\eapp{\ev{or}}{\ev x\ \ev y\ \ev z}]
		= \foldr(G,\, \false,\, \ev x\, \ev y\, \ev z)
		\\={}&
		\eho{\eho{\ev{or}}{\ev x}}{
			\eho{\eho{\ev{or}}{\ev y}}{
				\eho{\eho{\ev{or}}{\ev z}}{\false}}}
	\end{align*}
	%
	Similarly, since $Δ ⊢ \ev{=} ∷ \ch{\ev{and}}$,
	item~3d applies with
	$\mbf{chain}(\ev =,\, \ev a\, \ev b\, \ev c\, \ev d)
		= \eapp{\ev =}{\ev a\ \ev b}\,
		\eapp{\ev =}{\ev b\ \ev c}\,
		\eapp{\ev =}{\ev c\ \ev d}$,
	so:
	\begin{align*}
		 & \elab{Δ, \vec ρ}[\eapp{\ev{=}}{\ev a\ \ev b\ \ev c\ \ev d}]
		\\={}&
		\elab{Δ, \vec ρ}[\eapp{\ev{and}}{
				\eapp{\ev{=}}{\ev a\ \ev b}\
				\eapp{\ev{=}}{\ev b\ \ev c}\
				\eapp{\ev{=}}{\ev c\ \ev d}
			}]
	\end{align*}
	which is then elaborated recursively
	via the $\rcn\false$ attribute of $\ev{and}$.
\end{example}

\end{document}


% \subsection{Terms, Types and Attributes}
% %
% The \emph{built-in constants} of Eunoia are given by a fixed
% subset of symbols and denoted by the following expressions:
% $\mtt{Type}$ (the kind of all types),
% $\mtt{->}$ (arrow-type constructor),
% $\mtt{\_}$ (binary function application),
% $\mtt{Bool}$ (the type of Boolean values),
% $\texttt{true}$ and $\texttt{false}$ (terms of type \mtt{Bool}).
