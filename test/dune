; Per-phase unit tests: parsing, elaboration, encoding (Prelude), sig graph
(tests
 (names test_parsing test_encoding test_sig_graph)
 (libraries eo2lp unix)
 (deps ../src/Prelude.lp)
 (action (chdir %{project_root} (run %{test})))
)
